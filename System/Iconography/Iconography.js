export const Metadata = {
  Keyname: "Iconography",
  Title: "Iconography System",
  Note: "Alt name: Iconology, Iconism",
  Desc: "Provides icons for the user interface.",
  Icon: "icons",
  Utility: ["Aesthetic"],
  Status: "Standby",
};
export const Layout = {
  // 	Cover: function() {
  // 		const iconTypes = ["solid", "regular", "light", "thin", "duotone"];
  // 		var html =
  // 			genCoverIcons(["home", "user", "cloud"]) +
  // 			genCoverIcons(["file", "globe", "download"]) +
  // 			genCoverIcons(["film", "alarm-clock", "thumbs-up"]);
  // 		html = `<style>
  // .system-Iconography .content-Cover > div { display: flex; margin: 16px 16px 0; gap: 16px; line-height: 200%; }
  // .system-Iconography .content-Cover .Icon { margin-right: 4px; }
  // </style>
  // Activate this System to add icons to the user interface. The icon repository features over 30,000 icons to choose from with multiple styles.<div>${html}</div>`;
  // 		return html;
  // 		// genCoverIcons() - Generate a set of icons of different styles.
  // 		function genCoverIcons(iconNames) {
  // 			var html = "";
  // 			iconNames.forEach((name) => {
  // 				iconTypes.forEach((type) => {
  // 					html += `<i class="Icon fa-${name} fa-${type} fa-fw fa-xl"></i>`;
  // 				});
  // 				html += "<br>";
  // 			});
  // 			return `<div>${html}</div>`;
  // 		}
  // 	},
};
